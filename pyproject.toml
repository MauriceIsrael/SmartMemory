[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "semantic-memory-mcp"
version = "0.1.0"
dependencies = [
    "mcp",
    "rdflib",
    "pydantic",
    "requests",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
